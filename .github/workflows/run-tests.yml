name: Run Tests

on:
  pull_request:
    branches: [master]
  schedule:
    - cron: "0 7 * * 1-5" # Every Monday to Friday at 07:00 UTC
  workflow_dispatch:
    inputs:
      test_group:
        description: 'Test group to run'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - api
          - ui

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    # Always run on schedule/dispatch, detect changes on PR
    if: github.event_name != 'pull_request' || github.event_name == 'pull_request'
    outputs:
      api: ${{ steps.filter.outputs.api }}
      ui: ${{ steps.filter.outputs.ui }}
      should_run_api: ${{ steps.decide.outputs.api }}
      should_run_ui: ${{ steps.decide.outputs.ui }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Filter Paths
        if: github.event_name == 'pull_request'
        uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4.0.2
        id: filter
        with:
          filters: |
            api:
              - 'src/test/java/com/example/tests/api/**'
              - 'testng-api.xml'
              - 'pom.xml'
            ui:
              - 'src/test/java/com/example/tests/ui/**'
              - 'testng-ui.xml'
              - 'pom.xml'

      - name: Decide What to Run
        id: decide
        run: |
          # For scheduled runs or workflow_dispatch, run based on input or all
          if [[ "${{ github.event_name }}" == "schedule" ]]; then
            echo "api=true" >> $GITHUB_OUTPUT
            echo "ui=true" >> $GITHUB_OUTPUT
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            case "${{ inputs.test_group }}" in
              api)
                echo "api=true" >> $GITHUB_OUTPUT
                echo "ui=false" >> $GITHUB_OUTPUT
                ;;
              ui)
                echo "api=false" >> $GITHUB_OUTPUT
                echo "ui=true" >> $GITHUB_OUTPUT
                ;;
              *)
                echo "api=true" >> $GITHUB_OUTPUT
                echo "ui=true" >> $GITHUB_OUTPUT
                ;;
            esac
          else
            # For PRs, use path filter results (default to true if no filter ran)
            echo "api=${{ steps.filter.outputs.api || 'true' }}" >> $GITHUB_OUTPUT
            echo "ui=${{ steps.filter.outputs.ui || 'true' }}" >> $GITHUB_OUTPUT
          fi

  build:
    name: Build & Cache
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.should_run_api == 'true' || needs.changes.outputs.should_run_ui == 'true'
    outputs:
      cache-key: ${{ steps.cache-key.outputs.key }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Java
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Cache Maven Dependencies
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.m2/repository
            ~/.m2/wrapper
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Generate Cache Key
        id: cache-key
        run: echo "key=${{ runner.os }}-compiled-${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Check Compiled Cache
        id: compiled-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            target/classes
            target/test-classes
            target/generated-test-sources
          key: ${{ steps.cache-key.outputs.key }}

      - name: Compile Project
        if: steps.compiled-cache.outputs.cache-hit != 'true'
        run: mvn clean compile test-compile -B -q

      - name: Save Compiled Cache
        if: steps.compiled-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            target/classes
            target/test-classes
            target/generated-test-sources
          key: ${{ steps.cache-key.outputs.key }}

  test-api:
    name: API Tests
    needs: [changes, build]
    if: needs.changes.outputs.should_run_api == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Java
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Restore Maven Dependencies
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.m2/repository
            ~/.m2/wrapper
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Restore Compiled Classes
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            target/classes
            target/test-classes
            target/generated-test-sources
          key: ${{ needs.build.outputs.cache-key }}

      - name: Run API Tests
        id: tests
        run: mvn test -DsuiteXmlFile=testng-api.xml -B
        continue-on-error: true

      - name: Generate Test Summary
        if: always()
        uses: test-summary/action@37b508cfee6d4d080eedd00b5bb240a6a784a6a5 # v2.6
        with:
          paths: target/surefire-reports/TEST-*.xml
          show: all

      - name: Upload Test Reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-reports-api
          path: |
            target/surefire-reports/
            target/allure-results/
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload Test Logs on Failure
        if: failure() || steps.tests.outcome == 'failure'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-logs-api
          path: |
            target/surefire-reports/*.xml
            target/surefire-reports/*.txt
          retention-days: 14

      - name: Fail if Tests Failed
        if: steps.tests.outcome == 'failure'
        run: exit 1

  test-ui:
    name: UI Tests
    needs: [changes, build]
    if: needs.changes.outputs.should_run_ui == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Java
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Restore Maven Dependencies
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.m2/repository
            ~/.m2/wrapper
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Restore Compiled Classes
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            target/classes
            target/test-classes
            target/generated-test-sources
          key: ${{ needs.build.outputs.cache-key }}

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@2e1d749697dd1612b833dba4a722266286fbefcd #v2.1.2
        with:
          chrome-version: stable
          install-dependencies: true

      - name: Cache WebDriverManager
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/selenium
          key: ${{ runner.os }}-webdriver-chrome-stable
          restore-keys: |
            ${{ runner.os }}-webdriver-

      - name: Verify Chrome Installation
        run: |
          echo "Chrome version:"
          google-chrome --version
          echo "ChromeDriver will be managed by WebDriverManager"

      - name: Run UI Tests
        id: tests
        run: mvn test -DsuiteXmlFile=testng-ui.xml -B
        continue-on-error: true

      - name: Generate Test Summary
        if: always()
        uses: test-summary/action@37b508cfee6d4d080eedd00b5bb240a6a784a6a5 # v2.6
        with:
          paths: target/surefire-reports/TEST-*.xml
          show: all

      - name: Upload Test Reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-reports-ui
          path: |
            target/surefire-reports/
            target/allure-results/
            target/screenshots/
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload Test Logs on Failure
        if: failure() || steps.tests.outcome == 'failure'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-logs-ui
          path: |
            target/surefire-reports/*.xml
            target/surefire-reports/*.txt
            target/screenshots/
          retention-days: 14

      - name: Fail if Tests Failed
        if: steps.tests.outcome == 'failure'
        run: exit 1