name: Run Tests

on:
  pull_request:
    branches: [master]
  schedule:
    - cron: "0 7 * * 1-5" # Every Monday to Friday at 07:00 UTC
  workflow_dispatch:
    inputs:
      test_group:
        description: 'Test group to run'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - api
          - ui

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    # Always run on schedule/dispatch, detect changes on PR
    if: github.event_name != 'pull_request' || github.event_name == 'pull_request'
    outputs:
      api: ${{ steps.filter.outputs.api }}
      ui: ${{ steps.filter.outputs.ui }}
      should_run_api: ${{ steps.decide.outputs.api }}
      should_run_ui: ${{ steps.decide.outputs.ui }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Filter Paths
        if: github.event_name == 'pull_request'
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
        id: filter
        with:
          filters: |
            api:
              - 'src/test/java/com/example/tests/api/**'
              - 'testng-api.xml'
              - 'pom.xml'
            ui:
              - 'src/test/java/com/example/tests/ui/**'
              - 'testng-ui.xml'
              - 'pom.xml'

      - name: Decide What to Run
        id: decide
        run: |
          # For scheduled runs or workflow_dispatch, run based on input or all
          if [[ "${{ github.event_name }}" == "schedule" ]]; then
            echo "api=true" >> $GITHUB_OUTPUT
            echo "ui=true" >> $GITHUB_OUTPUT
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            case "${{ inputs.test_group }}" in
              api)
                echo "api=true" >> $GITHUB_OUTPUT
                echo "ui=false" >> $GITHUB_OUTPUT
                ;;
              ui)
                echo "api=false" >> $GITHUB_OUTPUT
                echo "ui=true" >> $GITHUB_OUTPUT
                ;;
              *)
                echo "api=true" >> $GITHUB_OUTPUT
                echo "ui=true" >> $GITHUB_OUTPUT
                ;;
            esac
          else
            # For PRs, use path filter results (default to true if no filter ran)
            echo "api=${{ steps.filter.outputs.api || 'true' }}" >> $GITHUB_OUTPUT
            echo "ui=${{ steps.filter.outputs.ui || 'true' }}" >> $GITHUB_OUTPUT
          fi

  build:
    name: Build & Cache
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.should_run_api == 'true' || needs.changes.outputs.should_run_ui == 'true'
    outputs:
      cache-key: ${{ steps.cache-key.outputs.key }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Java
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4.7.1
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Cache Maven Dependencies
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: |
            ~/.m2/repository
            ~/.m2/wrapper
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Generate Cache Key
        id: cache-key
        run: echo "key=${{ runner.os }}-compiled-${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Check Compiled Cache
        id: compiled-cache
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: |
            target/classes
            target/test-classes
            target/generated-test-sources
          key: ${{ steps.cache-key.outputs.key }}

      - name: Compile Project
        if: steps.compiled-cache.outputs.cache-hit != 'true'
        run: mvn clean compile test-compile -B -q

      - name: Save Compiled Cache
        if: steps.compiled-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: |
            target/classes
            target/test-classes
            target/generated-test-sources
          key: ${{ steps.cache-key.outputs.key }}

  test-api:
    name: API Tests
    needs: [changes, build]
    if: needs.changes.outputs.should_run_api == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Java
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4.7.1
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Restore Maven Dependencies
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: |
            ~/.m2/repository
            ~/.m2/wrapper
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Restore Compiled Classes
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: |
            target/classes
            target/test-classes
            target/generated-test-sources
          key: ${{ needs.build.outputs.cache-key }}

      - name: Run API Tests
        id: tests
        run: mvn test -DsuiteXmlFile=testng-api.xml -B
        continue-on-error: true

      - name: Generate Test Summary
        if: always()
        uses: test-summary/action@31493c76ec9e7aa675f1585d3ed6f1da69269a86 # v2.4
        with:
          paths: target/surefire-reports/TEST-*.xml
          show: all

      - name: Upload Test Reports
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: test-reports-api
          path: |
            target/surefire-reports/
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload Test Logs on Failure
        if: failure() || steps.tests.outcome == 'failure'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: test-logs-api
          path: |
            target/surefire-reports/*.xml
            target/surefire-reports/*.txt
          retention-days: 14

      - name: Fail if Tests Failed
        if: steps.tests.outcome == 'failure'
        run: exit 1

  test-ui:
    name: UI Tests
    needs: [changes, build]
    if: needs.changes.outputs.should_run_ui == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Java
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4.7.1
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Restore Maven Dependencies
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: |
            ~/.m2/repository
            ~/.m2/wrapper
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Restore Compiled Classes
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: |
            target/classes
            target/test-classes
            target/generated-test-sources
          key: ${{ needs.build.outputs.cache-key }}

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@b94431e051d1c52dcbe9a7092a4f10f827795416 #v2.1.0
        with:
          chrome-version: stable
          install-dependencies: true

      - name: Cache WebDriverManager
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ~/.cache/selenium
          key: ${{ runner.os }}-webdriver-chrome-stable
          restore-keys: |
            ${{ runner.os }}-webdriver-

      - name: Verify Chrome Installation
        run: |
          echo "Chrome version:"
          google-chrome --version
          echo "ChromeDriver will be managed by WebDriverManager"

      - name: Run UI Tests
        id: tests
        run: mvn test -DsuiteXmlFile=testng-ui.xml -B
        continue-on-error: true

      - name: Generate Test Summary
        if: always()
        uses: test-summary/action@31493c76ec9e7aa675f1585d3ed6f1da69269a86 # v2.4
        with:
          paths: target/surefire-reports/TEST-*.xml
          show: all

      - name: Upload Test Reports
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: test-reports-ui
          path: |
            target/surefire-reports/
            target/screenshots/
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload Test Logs on Failure
        if: failure() || steps.tests.outcome == 'failure'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: test-logs-ui
          path: |
            target/surefire-reports/*.xml
            target/surefire-reports/*.txt
            target/screenshots/
          retention-days: 14

      - name: Fail if Tests Failed
        if: steps.tests.outcome == 'failure'
        run: exit 1

  summary:
    name: Test Summary
    needs: [changes, test-api, test-ui]
    if: always() && (needs.changes.outputs.should_run_api == 'true' || needs.changes.outputs.should_run_ui == 'true')
    runs-on: ubuntu-latest
    steps:
      - name: Generate Summary
        run: |
          echo "### Test Execution Summary :test_tube:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Test Suite | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------------|--------|" >> $GITHUB_STEP_SUMMARY
          
          # API Tests
          if [[ "${{ needs.changes.outputs.should_run_api }}" == "true" ]]; then
            if [[ "${{ needs.test-api.result }}" == "success" ]]; then
              echo "| API Tests | :white_check_mark: Passed |" >> $GITHUB_STEP_SUMMARY
            elif [[ "${{ needs.test-api.result }}" == "failure" ]]; then
              echo "| API Tests | :x: Failed |" >> $GITHUB_STEP_SUMMARY
            else
              echo "| API Tests | :warning: ${{ needs.test-api.result }} |" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "| API Tests | :fast_forward: Skipped (no changes) |" >> $GITHUB_STEP_SUMMARY
          fi
          
          # UI Tests
          if [[ "${{ needs.changes.outputs.should_run_ui }}" == "true" ]]; then
            if [[ "${{ needs.test-ui.result }}" == "success" ]]; then
              echo "| UI Tests | :white_check_mark: Passed |" >> $GITHUB_STEP_SUMMARY
            elif [[ "${{ needs.test-ui.result }}" == "failure" ]]; then
              echo "| UI Tests | :x: Failed |" >> $GITHUB_STEP_SUMMARY
            else
              echo "| UI Tests | :warning: ${{ needs.test-ui.result }} |" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "| UI Tests | :fast_forward: Skipped (no changes) |" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Trigger:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

      - name: Check Overall Status
        if: needs.test-api.result == 'failure' || needs.test-ui.result == 'failure'
        run: |
          echo "One or more test suites failed"
          exit 1